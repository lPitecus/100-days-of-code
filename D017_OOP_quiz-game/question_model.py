class Question:

    def __init__(self, data_text, data_answer):
        # definirá ao atributo ".text" do objeto o argumento passado ao parâmetro
        # data_text (texto q virá do banco de dados)
        self.text = data_text
        # definirá ao atributo ".answer" do objeto o argumento passado ao parâmetro
        # data_answer (resposta que virá do banco de dados)
        self.answer = data_answer
